# Docs Landing Agent Notes

Follow `AGENTS.md` first. The reusable task procedures live in `.agents/skills`.

When changing landing/blog design, run the responsive Playwright screenshot checks described in `.agents/skills/landing-change/SKILL.md`.

When preparing a PR, follow `.agents/skills/submit-pr/SKILL.md`.
